# GMX MSD 分析工作流指南 (Step1-4)

> 更新日期: 2025-11-26  
> 作者: GitHub Copilot

---

## 📋 目录

1. [工作流概览](#工作流概览)
2. [脚本说明](#脚本说明)
3. [数据流向](#数据流向)
4. [筛选策略](#筛选策略)
5. [输出文件说明](#输出文件说明)
6. [使用示例](#使用示例)
7. [常见问题](#常见问题)

---

## 工作流概览

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        GMX MSD 分析工作流                                │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   原始数据 (.xvg)                                                       │
│        │                                                                │
│        ▼                                                                │
│   ┌────────────────────────────────────────┐                           │
│   │  Step1.2: 异常检测 + 集合平均           │  ← 核心脚本 (合并版)       │
│   │  step1_2_msd_analysis.py               │                           │
│   └────────────────────────────────────────┘                           │
│        │                                                                │
│        ├──→ large_D_outliers.csv (异常run清单)                         │
│        ├──→ ensemble_analysis_results.csv (筛选后)                     │
│        ├──→ ensemble_analysis_filtered.csv (异常组)                    │
│        └──→ ensemble_analysis_all.csv (不筛选)                         │
│                    │                                                    │
│        ┌──────────┴──────────┐                                         │
│        ▼                     ▼                                         │
│   ┌──────────────┐    ┌──────────────────────┐                        │
│   │  Step3:      │    │  Step4.1:            │                        │
│   │  绘制MSD曲线 │    │  D值综合分析         │                        │
│   └──────────────┘    └──────────────────────┘                        │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 脚本说明

### 1️⃣ step1_2_msd_analysis.py (核心脚本)

**功能**: 整合异常检测和集合平均，一站式完成数据质量筛选

**运行命令**:
```bash
python step1_2_msd_analysis.py
```

**筛选策略** (4级筛选):

| 规则 | 阈值 | 物理含义 |
|------|------|----------|
| 1. IQR异常检测 | Q1-1.5×IQR ~ Q3+1.5×IQR | D值统计离群 |
| 2. Intercept筛选 | > 10.0 A² | 结构松弛异常 |
| 3. D值上限筛选 | >= 5e-05 cm²/s | 团簇漂移异常 |
| 4. 联动筛选 | 同模拟任一元素异常 | 整个模拟标记 |

**输出文件**:

| 文件 | 级别 | 说明 |
|------|------|------|
| `large_D_outliers.csv` | run级 | 异常run清单 (供step3/step4使用) |
| `ensemble_analysis_results.csv` | 组级 | 筛选后数据 (step4.1默认读取) |
| `ensemble_analysis_filtered.csv` | 组级 | 异常runs的集合平均 |
| `ensemble_analysis_all.csv` | 组级 | 不筛选数据 (step4.1 --nofilter读取) |
| `ensemble_comparison.csv` | 组级 | 筛选前后对比 |
| `run_quality_report.txt` | 报告 | 质量分析报告 |

---

### 2️⃣ step3_plot_msd.py (MSD曲线绘制)

**功能**: 绘制各体系的MSD曲线，支持异常值筛选

**运行命令**:
```bash
# 默认模式 (启用筛选)
python step3_plot_msd.py

# 不筛选模式 (绘制所有曲线)
python step3_plot_msd.py --nofilter
```

**依赖文件**:
- `ensemble_analysis_results.csv` - 获取体系和温度列表
- `large_D_outliers.csv` - 标记异常run (可选)

**输出目录**: `results/msd_curves/`

**配置选项**:
```python
# 系统过滤配置 (可修改)
SYSTEM_FILTER = {
    'include_patterns': [r'^\d+$'],  # 只绘制纯数字命名的体系
    'exclude_patterns': [],           # 排除模式
}
```

---

### 3️⃣ step3_1_plot_msd_filtered.py (特定体系MSD绘制)

**功能**: 绘制特定体系的MSD曲线 (仅300K和900K)

**运行命令**:
```bash
python step3_1_plot_msd_filtered.py
```

**数据集配置** (修改 `DATASET` 变量):
```python
# 可选值: 'pt8sn6' 或 'air_86'
DATASET = 'air_86'  # 切换数据集
```

| 数据集 | 描述 | 数据路径 |
|--------|------|----------|
| `pt8sn6` | Pt8Sn6负载型数据 | `data/gmx_msd/unwrap/gmx_msd_results_20251118_152614` |
| `air_86` | 气象数据86系统 | `data/gmx_msd/unwrap/air/gmx_msd_results_20251124_170114` |

**过滤配置**:
```python
ENABLE_FILTERING = True   # 过滤异常runs
ENABLE_FILTERING = False  # 绘制所有runs
```

**输出目录**: `results/msd_curves_<dataset>/`

---

### 4️⃣ step4_1_D_value_comprehensive_analysis.py (D值综合分析)

**功能**: 从CSV读取D值，进行统计分析和可视化

**运行命令**:
```bash
# 默认模式 (使用筛选后数据)
python step4_1_D_value_comprehensive_analysis.py

# 不筛选模式 (使用完整数据)
python step4_1_D_value_comprehensive_analysis.py --nofilter

# 排除特定结构
python step4_1_D_value_comprehensive_analysis.py --exclude Pt8Sn0
```

**数据源**:
| 模式 | 读取文件 |
|------|----------|
| 默认 | `ensemble_analysis_results.csv` |
| --nofilter | `ensemble_analysis_all.csv` |

**D值流动性分类**:
| D值范围 | 分类 |
|---------|------|
| D < 1e-8 cm²/s | 低流动性 (Low Mobility) |
| 1e-8 < D < 1e-7 | 中等流动性 (Medium Mobility) |
| D > 1e-7 | 高流动性 (High Mobility) |

**输出目录**: `results/D_value_comprehensive/`

**输出文件**:
- `ensemble_D_values.csv` - D值汇总表
- `D_value_statistics.csv` - 统计数据 (含R²字段)
- `D_vs_T_*.png` - D-T关系图
- `D_value_heatmap_*.png` - D值热力图
- `R2_heatmap_*.png` - R²热力图 (拟合质量检查)
- `transition_temperature_*.png` - 转变温度分析
- `D_analysis_report.txt` - 综合报告

---

## 数据流向

```
原始数据: 360 runs (每组6个run × 60组)
         │
         ▼ step1_2 筛选
         │
    ┌────┴────┐
    │         │
    ▼         ▼
有效runs    异常runs
27 runs     333 runs (92.5%)
    │         │
    ▼         ▼
  18组       60组
    │         │
    ▼         ▼
ensemble_   ensemble_
analysis_   analysis_
results.csv filtered.csv
    │
    │ + 不筛选版 (360 runs → 60组)
    │
    ▼
ensemble_analysis_all.csv
```

**关键概念**:
- **Run**: 单次模拟轨迹 (如 T1000.r24.gpu0_msd_Pt.xvg)
- **组**: (体系 × 温度 × 元素) 的组合
- **集合平均**: 对同一组的多个run求MSD曲线平均，再拟合D值

---

## 筛选策略

### 统一阈值配置

当前 `step1_2_msd_analysis.py` 使用的阈值:

```python
# 异常检测参数
OUTLIER_METHOD = 'IQR'     # 可选: 'IQR', '3sigma', 'MAD'
IQR_MULTIPLIER = 1.5       # IQR方法的倍数

# run级别筛选
INTERCEPT_MAX = 10.0       # A² - 结构松弛阈值
D_MAX_THRESHOLD = 5e-5     # cm²/s - 团簇漂移阈值

# 拟合参数
FIT_START_RATIO = 0.1      # MSD拟合起点 (10%)
FIT_END_RATIO = 0.9        # MSD拟合终点 (90%)
```

### 筛选结果示例

```
[筛选策略] 4级run质量筛选:
---------------------------------------------------------------
| 规则 | 阈值 | 物理含义 | 筛除runs |
---------------------------------------------------------------
| 1. IQR异常检测 | Q1-1.5*IQR ~ Q3+1.5*IQR | D值统计离群 | 26 runs |
| 2. Intercept筛选 | > 10.0 A^2 | 结构松弛异常 | 237 runs |
| 3. D值上限筛选 | >= 5e-05 cm^2/s | 团簇漂移异常 | 0 runs |
| 4. 联动筛选 | 同模拟任一元素异常 | 整个模拟标记 | 70 runs |
---------------------------------------------------------------
| 合计 | - | - | 333 runs (92.5%) |
---------------------------------------------------------------
```

---

## 输出文件说明

### results/ 目录结构

```
results/
├── large_D_outliers.csv              # 异常run清单 (333条)
├── ensemble_analysis_results.csv     # 筛选后集合平均 (18组)
├── ensemble_analysis_filtered.csv    # 异常runs集合平均 (60组)
├── ensemble_analysis_all.csv         # 不筛选集合平均 (60组)
├── ensemble_comparison.csv           # 筛选效果对比
├── run_quality_report.txt            # 质量报告
│
├── msd_curves/                       # step3 输出
│   ├── 68_all_temps_GMX.png
│   └── filtering_statistics.txt
│
├── msd_curves_air_86/                # step3_1 输出 (air_86)
│   └── 86_300K_900K_filtered_*.png
│
└── D_value_comprehensive/            # step4.1 输出
    ├── D_value_statistics.csv
    ├── D_vs_T_*.png
    ├── D_value_heatmap_*.png
    ├── R2_heatmap_*.png
    └── D_analysis_report.txt
```

### CSV文件字段说明

**large_D_outliers.csv**:
| 字段 | 说明 |
|------|------|
| group_key | 组合键 (composition_temperature_element) |
| composition | 体系组成 |
| temperature | 温度 |
| element | 元素 (Pt/Sn) |
| run_id | run编号 |
| gmx_D | GMX计算的D值 |
| filepath | 文件路径 |
| reason | 筛除原因 |

**ensemble_analysis_results.csv**:
| 字段 | 说明 |
|------|------|
| composition | 体系组成 |
| temperature | 温度 |
| temp_value | 温度数值 (K) |
| element | 元素 |
| n_runs | 用于集合平均的run数 |
| n_runs_original | 原始run数 |
| n_runs_filtered | 被筛除的run数 |
| D_ensemble | 集合平均后的D值 (cm²/s) |
| R2_ensemble | 拟合R² |
| intercept | 拟合截距 (A²) |

---

## 使用示例

### 完整工作流

```bash
# Step 1: 运行核心分析 (异常检测 + 集合平均)
python step1_2_msd_analysis.py

# Step 2: 绘制MSD曲线
python step3_plot_msd.py           # 全部体系
python step3_1_plot_msd_filtered.py  # 特定体系

# Step 3: D值综合分析
python step4_1_D_value_comprehensive_analysis.py
```

### 不筛选模式

```bash
# 使用完整数据 (含异常)
python step4_1_D_value_comprehensive_analysis.py --nofilter
python step3_plot_msd.py --nofilter
```

### 修改筛选阈值

编辑 `step1_2_msd_analysis.py`:
```python
# 宽松模式
INTERCEPT_MAX = 20.0  # 原10.0

# 严格模式
INTERCEPT_MAX = 5.0   # 更严格
```

然后重新运行:
```bash
python step1_2_msd_analysis.py
```

---

## 常见问题

### Q1: 为什么92.5%的runs被筛除？

**A**: 主要原因是 `Intercept > 10.0 A²` 筛除了 237 runs (71.8%)。这表明大部分run存在结构松弛问题。可以考虑:
1. 放宽阈值 (`INTERCEPT_MAX = 20.0`)
2. 检查模拟初始结构是否充分平衡

### Q2: large_D_outliers.csv 和 ensemble_analysis_filtered.csv 有什么区别？

**A**: 
- `large_D_outliers.csv`: **run级别** (333条记录)
- `ensemble_analysis_filtered.csv`: **组级别** (60组，用333个异常runs集合平均)

### Q3: ensemble_analysis_all.csv 应该有60组还是78组？

**A**: 应该是 **60组**。这是用全部360 runs (不筛选) 做集合平均的结果。

### Q4: step3 和 step4 如何选择数据源？

**A**:
| 脚本 | 默认数据源 | --nofilter数据源 |
|------|------------|------------------|
| step3 | `ensemble_analysis_results.csv` | 文件索引直接扫描 |
| step4.1 | `ensemble_analysis_results.csv` | `ensemble_analysis_all.csv` |

### Q5: 如何添加新的数据集？

**A**: 修改脚本中的 `GMX_DATA_DIRS`:
```python
GMX_DATA_DIRS = [
    BASE_DIR / 'data' / 'gmx_msd' / 'unwrap' / 'new_dataset'
]
```

---

## 版本历史

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| v2.0 | 2025-11-26 | 合并step1和step2为step1_2 |
| v1.0 | 2025-11-25 | 初始版本 |

---

## 联系方式

如有问题，请检查:
1. 数据路径是否正确
2. CSV文件是否存在
3. Python环境是否正确配置

